import SwiftUIimport AVKit
struct VideoPageView: View {    let images = ["0", "1", "2", "3", "4", "5"]    @State private var currentImageIndex = 0    @State private var player = AVPlayer()    @State private var timer: Timer?        var body: some View {        VStack {            Text("Videos to Watch")                .font(.largeTitle)                .padding(.bottom, 20)                        VideoPlayer(player: player)                .frame(height: 300)                .cornerRadius(15)                 .padding(.bottom, 20)                .onAppear {                    startPlayback()                }                .onDisappear {                    stopPlayback()                }                        Image(images[currentImageIndex])                .resizable()                .aspectRatio(contentMode: .fill)                .frame(height: 300) // Set height for the slideshow                .cornerRadius(15)                .padding()                        Spacer()        }        .padding()    }        private func startPlayback() {        if let url = Bundle.main.url(forResource: "Video4", withExtension: "mp4") {            player.replaceCurrentItem(with: AVPlayerItem(url: url))            player.play()            setupTimer()        }    }        private func stopPlayback() {        player.pause()        timer?.invalidate()    }        private func setupTimer() {        timer = Timer.scheduledTimer(withTimeInterval: 1.0, repeats: true) { _ in            updateImageBasedOnTime()        }    }        private func updateImageBasedOnTime() {        let currentTime = player.currentTime().seconds                // Show images at specific times        switch currentTime {        case _ where currentTime >= 20 && currentTime < 23:            currentImageIndex = 0 // "row" at 0:15        case _ where currentTime >= 23 && currentTime < 25:            currentImageIndex = 1 // "ur boat" at 0:18        case _ where currentTime >= 25 && currentTime < 28:            currentImageIndex = 2 // "gently" at 0:21        case _ where currentTime >= 29 && currentTime < 32:            currentImageIndex = 3 // "merrily" at 0:22        case _ where currentTime >= 32  && currentTime < 34:            currentImageIndex = 4 // "life" at 0:24        case _ where currentTime >= 35 && currentTime < 36:            currentImageIndex = 5 // "dream" at 0:26        case _ where currentTime >= 36 && currentTime < 38:            currentImageIndex = 0 // "row" at 0:15        case _ where currentTime >= 38 && currentTime < 40:            currentImageIndex = 1 // "ur boat" at 0:18        case _ where currentTime >= 40 && currentTime < 43:            currentImageIndex = 2 // "gently" at 0:21        case _ where currentTime >= 43 && currentTime < 47:            currentImageIndex = 3 // "merrily" at 0:22        case _ where currentTime >= 47  && currentTime < 50:            currentImageIndex = 4 // "life" at 0:24        case _ where currentTime >= 50 && currentTime < 51:            currentImageIndex = 5 // "dream" at 0:26        case _ where currentTime >= 51 && currentTime < 69:            currentImageIndex = 0 // "row" at 0:15        case _ where currentTime >= 69 && currentTime < 71:            currentImageIndex = 1 // "ur boat" at 0:18        case _ where currentTime >= 71 && currentTime < 73:            currentImageIndex = 2 // "gently" at 0:21        case _ where currentTime >= 73 && currentTime < 78:            currentImageIndex = 3 // "merrily" at 0:22        case _ where currentTime >= 78  && currentTime < 80:            currentImageIndex = 4 // "life" at 0:24        case _ where currentTime >= 80 && currentTime < 82:            currentImageIndex = 5 // "dream" at 0:26        case _ where currentTime >= 82 && currentTime < 84:            currentImageIndex = 0 // "row" at 0:15        case _ where currentTime >= 84 && currentTime < 86:            currentImageIndex = 1 // "ur boat" at 0:18        case _ where currentTime >= 86 && currentTime < 90:            currentImageIndex = 2 // "gently" at 0:21        case _ where currentTime >= 90 && currentTime < 93:            currentImageIndex = 3 // "merrily" at 0:22        case _ where currentTime >= 93  && currentTime < 94:            currentImageIndex = 4 // "life" at 0:24        case _ where currentTime >= 94 && currentTime < 97:            currentImageIndex = 5 // "dream" at 0:26        default:            break        }    }}
struct VideoPageView_Previews: PreviewProvider {    static var previews: some View {        VideoPageView()    }}

